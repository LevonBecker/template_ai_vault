---
name: setup
description: Use for initial template_ai_vault project setup — creates the uv virtual environment and installs dependencies. Equivalent to /setup.
---

# Setup Workflow

Use this file as source of truth: `.github/prompts/setup.prompt.md`

When the user asks to set up the project, read that prompt file and follow it.
