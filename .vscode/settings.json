{
    "chat.tools.terminal.autoApprove": {
        "uv run --no-sync": true,
        "uv run --no-sync python -m modules.repo.route \"view_screenshot\"": true,
        "!`uv run --no-sync python -m modules.repo.route \"view_screenshot\"`": true
    },
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
}
